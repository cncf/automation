apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bomhort-secret
  namespace: bomhort
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: oci-secret-store
  target:
    name: bomhort-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: seebom-secret
