name: ""  # This should trigger a validation error
description: ""  # This should trigger a validation error
# Missing maturity_log - should trigger error
repositories: []  # Empty - should trigger error
social:
  twitter: "@badproject"
artwork: "not-a-url"  # Invalid URL
website: "also-not-a-url"  # Invalid URL
audits:
  - date: "invalid-date"  # Invalid date format
    type: ""  # Empty type
    url: "not-a-url"  # Invalid URL
